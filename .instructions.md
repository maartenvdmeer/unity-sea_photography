# Coding Preferences & Style Guide

## Python Function Formatting

Reference implementation from `blender/species/algae_eater.py`:

### Structure
- Type hints on parameters and return values
- Clear parameter descriptions in docstrings
- Comments explaining the mathematical/biological logic
- Concise variable names with biological context where relevant

### Template
```python
def get_body_vertex(t: float, angle: float, length: float) -> tuple[float, float, float]:
    """
    Calculate 3D vertex position along fish body.
    
    Args:
        t: Normalized position along body (0.0 to 1.0), where 0 is head
        angle: Rotation angle in radians around the y-axis
        length: Total body length in model units
    
    Returns:
        Tuple of (x, y, z) coordinates
    
    Biology:
        - Flattened belly matches bottom-feeding behavior (sucker mouth)
        - Sinusoidal curves create natural body geometry
    """
    # Implementation here